% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PPI_cluster.R
\name{run_louvain}
\alias{run_louvain}
\title{Compute Louvain Clustering for PPI Network}
\usage{
run_louvain(g, resolution = 1, weights = NULL)
}
\arguments{
\item{g}{An \code{igraph} object.}

\item{resolution}{Numeric. The resolution parameter controls the granularity of clusters. Default is 1.0 (standard modularity).}

\item{weights}{Numeric vector or NULL. Edge weights to use for clustering.
If NULL (default), the function attempts to use the 'weight' or 'score' edge attribute.
Set to NA to perform unweighted clustering.}
}
\value{
The input \code{igraph} object with a new vertex attribute \code{louvain_cluster}.
}
\description{
This function performs Louvain clustering (multi-level modularity optimization)
on an igraph object. It is suitable for detecting larger, macro-scale functional
modules in PPI networks.
}
\examples{
data(demo_ppi)
library(igraph)
ppi <- run_louvain(demo_ppi, resolution = 1)
head(V(ppi)$louvain_cluster)

}
