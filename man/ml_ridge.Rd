% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ml_models.r
\name{ml_ridge}
\alias{ml_ridge}
\title{Ridge regression feature selection (a wrapper function from \code{\link[=ml_enet]{ml_enet()}})
Calls \code{\link[=ml_enet]{ml_enet()}} with \code{alpha = 0}.
Caution: Ridge does not shrink coefficients to zero; when \code{top_n = NULL}
(default), all features are kept. Use \code{\link[=select_features]{select_features()}} after inspecting importance to trim.}
\usage{
ml_ridge(
  ml_data,
  lambda_rule = c("1se", "min"),
  top_n = NULL,
  cv_folds = 10,
  seed = 2025,
  ...
)
}
\arguments{
\item{ml_data}{A \code{tcm_ml_data} object from \code{\link[=prepare_ml_data]{prepare_ml_data()}}.}

\item{lambda_rule}{\code{"1se"} (parsimonious) or \code{"min"} (best AUC).}

\item{top_n}{For Ridge: max number of top genes to keep (by
\code{abs(coefficient)}). Default \code{NULL} (keep all).
Ignored for LASSO / Elastic Net.}

\item{cv_folds}{Number of CV folds. Default 10.}

\item{seed}{Random seed. Default 2025.}

\item{...}{Passed to \code{\link[glmnet:cv.glmnet]{glmnet::cv.glmnet()}}.}
}
\value{
A \code{tcm_ml} object (method = "ridge").
}
\description{
Ridge regression feature selection (a wrapper function from \code{\link[=ml_enet]{ml_enet()}})
Calls \code{\link[=ml_enet]{ml_enet()}} with \code{alpha = 0}.
Caution: Ridge does not shrink coefficients to zero; when \code{top_n = NULL}
(default), all features are kept. Use \code{\link[=select_features]{select_features()}} after inspecting importance to trim.
}
\examples{
\dontrun{
  ml_data <- prepare_ml_data(expr_mat, group)
  res <- ml_ridge(ml_data)
}
}
