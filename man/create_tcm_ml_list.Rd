% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ml_models.r
\name{create_tcm_ml_list}
\alias{create_tcm_ml_list}
\title{Assemble individually fitted ML models into a \code{tcm_ml_list}}
\usage{
create_tcm_ml_list(...)
}
\arguments{
\item{...}{Named or unnamed \code{tcm_ml} objects. Names become the method labels used in downstream plots. At least one object must be supplied.}
}
\value{
A \code{tcm_ml_list} object identical in structure to the output of \code{\link{run_ml_screening}}, with the \code{"ml_data"} attribute set from the first element.
}
\description{
Assemble individually fitted ML models into a \code{tcm_ml_list}
}
\examples{
\dontrun{
  ml_data <- prepare_ml_data(expr_mat, group)
  res_lasso <- ml_lasso(ml_data)
  res_rf <- ml_rf(ml_data)
  res_svm <- ml_svm_rfe(ml_data)

  ## assemble into a tcm_ml_list
  ml_list <- create_tcm_ml_list(
    lasso   = res_lasso,
    rf      = res_rf,
    svm_rfe = res_svm)
}
}
