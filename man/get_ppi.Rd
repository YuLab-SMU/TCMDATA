% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PPI_utils.R
\name{get_ppi}
\alias{get_ppi}
\title{Retrieve a STRING PPI network}
\usage{
get_ppi(x, taxID = "auto", ...)
}
\arguments{
\item{x}{An \code{enrichResult} object or a character vector of proteins.}

\item{taxID}{NCBI taxonomy identifier. Use \code{"auto"} to let
\code{clusterProfiler::getPPI()} infer it from an \code{enrichResult}
when possible.}

\item{...}{Additional arguments passed to \code{clusterProfiler::getPPI()},
such as \code{ID}, \code{required_score}, \code{network_type},
\code{add_nodes}, \code{show_query_node_labels}, and \code{output}.}
}
\value{
An \code{igraph} object or a \code{data.frame}, depending on the
arguments passed through \code{...}.
}
\description{
Thin wrapper around \code{clusterProfiler::getPPI()} so the function is
available directly from TCMDATA.
}
\examples{
\dontrun{
  genes <- c("TP53", "BRCA1", "AKT1", "EGFR")
  g <- get_ppi(genes, taxID = 9606)
}

}
