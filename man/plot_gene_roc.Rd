% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ml_gene_diag.R
\name{plot_gene_roc}
\alias{plot_gene_roc}
\title{ROC curves for individual diagnostic genes}
\usage{
plot_gene_roc(
  genes,
  ml_data = NULL,
  expr_mat = NULL,
  group = NULL,
  use = c("auto", "train", "test"),
  combine = TRUE,
  palette = NULL,
  show_ci = FALSE,
  ncol = NULL,
  label_size = 9,
  p_adjust_method = "BH"
)
}
\arguments{
\item{genes}{Character vector of gene symbols to evaluate.}

\item{ml_data}{A \code{tcm_ml_data} object from \code{\link{prepare_ml_data}}
(preferred input).  When provided, \code{expr_mat} / \code{group} are
ignored.}

\item{expr_mat}{Numeric matrix (genes \eqn{\times} samples) with
\code{rownames} = gene symbols.  Used only when \code{ml_data} is
\code{NULL}.}

\item{group}{Factor or character vector of sample labels (length =
\code{ncol(expr_mat)}, two levels).}

\item{use}{Which data split to evaluate when using \code{ml_data}:
\code{"auto"} (default; test set if available, else train),
\code{"train"}, or \code{"test"}.}

\item{combine}{Logical; if \code{TRUE} (default) all curves are overlaid
on one panel.  If \code{FALSE} each gene gets its own facet.}

\item{palette}{Character vector of colours (one per gene), or \code{NULL}
for the built-in 12-colour publication palette.}

\item{show_ci}{Logical; add a translucent 95 \\% CI band around each curve?
Default \code{FALSE}.  Requires \code{pROC::ci.se()}.}

\item{ncol}{Integer; number of columns when \code{combine = FALSE}.
Default \code{NULL} (auto).}

\item{label_size}{Legend text size. Default 9.}

\item{p_adjust_method}{Method for multiple-testing correction passed to
\code{\link[stats]{p.adjust}}.  Default \code{"BH"} (Benjamini–Hochberg
FDR).  Other common choices: \code{"bonferroni"}, \code{"holm"},
\code{"none"}.  When only a single gene is supplied no correction is
needed and \code{p_adj} equals \code{p_value} regardless of method.}
}
\value{
A \code{ggplot} object.
\code{attr(, "auc_table")} contains the same data.frame produced by
\code{\link{get_gene_auc}}.
}
\description{
Overlays one ROC curve per gene on a single panel (\code{combine = TRUE},
default), or draws one ROC per facet (\code{combine = FALSE}).
This is the natural follow-up after \code{\link{get_ml_consensus}}: the
consensus genes are fed back to evaluate their individual diagnostic power.
}
\examples{
\dontrun{
  consensus <- get_ml_consensus(ml_list, min_methods = 2)
  plot_gene_roc(consensus, ml_data = ml_data)
  plot_gene_roc(consensus, ml_data = ml_data, combine = FALSE)
}
}
