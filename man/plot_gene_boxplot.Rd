% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ml_gene_diag.R
\name{plot_gene_boxplot}
\alias{plot_gene_boxplot}
\title{Two-group expression boxplots for diagnostic genes}
\usage{
plot_gene_boxplot(
  genes,
  ml_data = NULL,
  expr_mat = NULL,
  group = NULL,
  use = c("auto", "train", "test"),
  test_method = c("wilcox", "t.test"),
  palette = c("#E74C3C", "#3498DB"),
  show_points = TRUE,
  point_alpha = 0.35,
  violin = FALSE,
  ncol = NULL,
  scales = "free_y",
  p_label = c("p.format", "p.signif", "p.adj"),
  p_adjust_method = "BH",
  base_size = 12
)
}
\arguments{
\item{genes}{Character vector of gene symbols to evaluate.}

\item{ml_data}{A \code{tcm_ml_data} object from \code{\link{prepare_ml_data}}
(preferred input).  When provided, \code{expr_mat} / \code{group} are
ignored.}

\item{expr_mat}{Numeric matrix (genes \eqn{\times} samples) with
\code{rownames} = gene symbols.  Used only when \code{ml_data} is
\code{NULL}.}

\item{group}{Factor or character vector of sample labels (length =
\code{ncol(expr_mat)}, two levels).}

\item{use}{Which data split to evaluate when using \code{ml_data}:
\code{"auto"} (default; test set if available, else train),
\code{"train"}, or \code{"test"}.}

\item{test_method}{Statistical test: \code{"wilcox"} (Wilcoxon rank-sum,
default) or \code{"t.test"}.}

\item{palette}{Character(2); fill colours for the two groups.
Default \code{c("#E74C3C", "#3498DB")} (red / blue).}

\item{show_points}{Logical; overlay jittered data points? Default \code{TRUE}.}

\item{point_alpha}{Alpha transparency for jittered points. Default 0.35.}

\item{violin}{Logical; add a violin layer behind the boxes?
Default \code{FALSE}.}

\item{ncol}{Integer; number of columns in \code{facet_wrap}.
Default \code{NULL} (auto-computed).}

\item{scales}{Passed to \code{\link[ggplot2]{facet_wrap}}.
Default \code{"free_y"}.}

\item{p_label}{How to annotate p-values: \code{"p.format"} (numeric,
default), \code{"p.signif"} (stars: ns / * / ** / *** / ****), or
\code{"p.adj"} (adjusted, numeric).}

\item{p_adjust_method}{Method for multiple-testing correction passed to
\code{\link[stats]{p.adjust}}.  Default \code{"BH"}.  Applies both to the
\code{p_adj} column in \code{attr(, "test_table")} and to the annotation
when \code{p_label = "p.adj"}.  When only a single gene is tested,
\code{p_adj} equals \code{p_value} regardless of method.}

\item{base_size}{Base font size for \code{\link[ggplot2]{theme_bw}}.
Default 12.}
}
\value{
A \code{ggplot} object.
\code{attr(, "test_table")} contains a \code{data.frame}:
\code{gene}, \code{statistic}, \code{p_value}, \code{p_adj},
\code{method}, \code{group1_mean}, \code{group2_mean}, \code{log2FC}.
}
\description{
Draws a faceted boxplot comparing expression levels between two groups for
each gene, with optional jittered points, violin overlay and statistical
test annotation.  Designed for publication-quality figures with zero extra
dependencies beyond \pkg{ggplot2} and base R \code{stats}.
}
\examples{
\dontrun{
  consensus <- get_ml_consensus(ml_list, min_methods = 2)
  plot_gene_boxplot(consensus, ml_data = ml_data)
  plot_gene_boxplot(consensus, expr_mat = expr_mat, group = group,
                    violin = TRUE, p_label = "p.signif")
}
}
