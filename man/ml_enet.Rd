% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ml_models.r
\name{ml_enet}
\alias{ml_enet}
\title{Elastic Net feature selection via cv.glmnet
Penalised logistic regression with configurable alpha.
\itemize{
\item alpha = 1 : LASSO
\item alpha = 0 : Ridge
\item 0 < alpha < 1 : Elastic Net
}
Caution: For Ridge (\code{alpha = 0}), all coefficients are non-zero.
When \code{top_n = NULL} (default), all features are retained so that
users can inspect \code{$importance} first, then call
\code{\link[=select_features]{select_features()}} to trim to the desired number.}
\usage{
ml_enet(
  ml_data,
  alpha = 0.5,
  lambda_rule = c("1se", "min"),
  top_n = NULL,
  type_measure = "auc",
  cv_folds = 10,
  seed = 2025,
  ...
)
}
\arguments{
\item{ml_data}{A \code{tcm_ml_data} object from \code{\link[=prepare_ml_data]{prepare_ml_data()}}.}

\item{alpha}{Mixing parameter, 0 to 1. Default 0.5.}

\item{lambda_rule}{\code{"1se"} (parsimonious) or \code{"min"} (best AUC).}

\item{top_n}{For Ridge: max number of top genes to keep (by
\code{abs(coefficient)}). Default \code{NULL} (keep all).
Ignored for LASSO / Elastic Net.}

\item{type_measure}{Loss function for CV in \code{\link[glmnet:cv.glmnet]{glmnet::cv.glmnet()}}.
Default \code{"auc"}. Other options: \code{"deviance"},
\code{"class"}, \code{"mse"}, \code{"mae"}.}

\item{cv_folds}{Number of CV folds. Default 10.}

\item{seed}{Random seed. Default 2025.}

\item{...}{Passed to \code{\link[glmnet:cv.glmnet]{glmnet::cv.glmnet()}}.}
}
\value{
A \code{tcm_ml} object.
}
\description{
Elastic Net feature selection via cv.glmnet
Penalised logistic regression with configurable alpha.
\itemize{
\item alpha = 1 : LASSO
\item alpha = 0 : Ridge
\item 0 < alpha < 1 : Elastic Net
}
Caution: For Ridge (\code{alpha = 0}), all coefficients are non-zero.
When \code{top_n = NULL} (default), all features are retained so that
users can inspect \code{$importance} first, then call
\code{\link[=select_features]{select_features()}} to trim to the desired number.
}
\examples{
\dontrun{
  ml_data <- prepare_ml_data(expr_mat, group)
  res <- ml_enet(ml_data)
}
}
